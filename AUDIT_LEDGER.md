# Repair audit — 2026-09-06 to 2026-09-07

Baseline: clean worktree at a45315e; 34 tests, formatting, lint, type checking and build passed.
Passing baseline tests did not establish the properties below. The repaired working tree remains uncommitted and unpushed. The current version, including browser-driven serverless ticks and Nokia test-network integration, was deployed to the existing Vercel production project on September 9 after the user explicitly authorized deployment.

| Finding                                                 | Evidence and consequence                                                                                                                           | Repair / verification                                                                                                                                                                                                                                                          |
| ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| High: unknown evidence counted as compromise            | `decision-engine.ts` counted unavailable/missing evidence in containment anomaly count. Unsafe command plus outage could isolate a gateway.        | Repaired. Unknown evidence fails authorization but is excluded from confirmed anomaly count. Degraded-provider integration and browser runs end at `BLOCK` with no enforcement.                                                                                                |
| High: no role check in authoritative decision           | A valid VIEWER principal could obtain ALLOW for a setpoint.                                                                                        | Repaired. Deterministic policy requires `PRIVILEGED_OPERATOR` or `OT_SECURITY_SUPERVISOR` for `SET_PRESSURE`; unit coverage passes.                                                                                                                                            |
| High: process measurements fabricated from story steps  | `advanceHeartbeats` incremented both counters and alternated pressure per workflow event; ALLOW assigned requested directly to actual.             | Repaired. The twin advances from elapsed observation time, keeps requested/accepted/actual values separate, and revalidates immediately before an ALLOW dispatch. No physical dispatch adapter exists.                                                                         |
| High: enforcement accepted as confirmed                 | Nokia delete attachment response and non-UNAVAILABLE QoD status were recorded SUCCEEDED.                                                           | Repaired. Detach requires `DETACHED`; QoD requires `AVAILABLE`. Unknown results remain unconfirmed and visible.                                                                                                                                                                |
| High: replay/restart side effects and races             | Enforcement invoked before store deduplication; event/run writes separate; Reset can race pending evidence.                                        | Repaired for this single-process prototype. Intent precedes side effects, uncertain intents are not blindly retried, transitions commit transactionally, and busy controls reject races.                                                                                       |
| High: playback and physical state used different clocks | Proof cadence scaled with playback speed, but process telemetry advanced on wall time even while paused.                                           | Repaired. One pausable backend simulation clock drives fixed-step process integration, simulation timestamps, heartbeats, proof scheduling, and UI motion parameters at 0.5×/1×/2×/4×. External calls remain wall-clock based.                                                 |
| High: backup ownership preceded confirmed handover      | Gateway detachment immediately selected the backup, and a failed QoD could still leave the UI claiming backup control.                             | Repaired. Detachment only isolates the primary path. Backup ownership requires successful QoD plus simulated safe-control activation; otherwise the modeled pump safe-stops.                                                                                                   |
| Presentation: no visible safe-change contrast           | The original judge path demonstrated only an unsafe request; process motion was not connected to a coherent facility view.                         | Repaired. The state-bound SVG shows tanks, pump, pipes, treatment, valve, command/evidence traffic, isolation, and controller ownership. A safe 52% change visibly responds before an 88% request is blocked.                                                                  |
| High: Vercel froze automatic playback after responses   | Vercel's serverless runtime does not keep the local `setInterval` simulation driver active between requests, so production commands remained held. | Repaired. While playback is active, the browser sends a bounded 750 ms simulation tick through the authenticated control route. The backend remains the authoritative clock and fixed-step process integrator.                                                                 |
| High: LIVE demo session grants supervisor               | Anonymous session endpoint issues supervisor role in every mode; fixture principal not production identity binding.                                | Contained. Every interactive `/api/` route is DEMO-only. SANDBOX/LIVE need enterprise authentication and durable command-to-gateway binding before they can be enabled.                                                                                                        |
| Medium: STEP_UP resolves held command as BLOCKED        | No approval flow exists. No supported production step-up authorization.                                                                            | Repaired to remain held as `STEP_UP`. No approval or recovery endpoint is claimed.                                                                                                                                                                                             |
| External verification was unavailable                   | Earlier reviews had no Nokia or hosted-model credentials.                                                                                          | Partially repaired. Public calls now verify Nokia test-network SIM/device swap, location, reachability, and QoD lifecycle behavior plus hosted reasoning. Number Verification still lacks subscriber OAuth; no slice attachment, operator-core effect, or physical PLC exists. |

## Final local verification

- `npm run verify`: passed on September 9. Prettier, ESLint, server/web type checks, 67 Vitest tests, and the production build passed.
- `npm run test:e2e`: 18 passed and 2 intentionally skipped. The complete safe-then-unsafe interaction passes on desktop and projector; baseline, keyboard, incident, serious/critical accessibility, and browser-console checks pass across desktop, projector, tablet, and mobile.
- `npm run audit:deps`: passed with 0 vulnerabilities reported by npm.
- Fresh browser rehearsal on September 7 at 2×: three consecutive standard runs completed in 44.410–44.516 seconds and produced `BLOCK_AND_CONTAIN`; the degraded-provider run completed in 35.224 seconds and produced `BLOCK` with zero network-enforcement records. Every run recorded zero unsafe executions, safe-band pressure, nonzero observation duration, and backup heartbeats. Exact measurements and correlation IDs are in `artifacts/browser-rehearsals.json`.
- `npm run visual:qa`: passed. Safe-response, blocked-decision, and incident captures were regenerated across desktop, projector, tablet, and mobile. The final allowed desktop and allowed/blocked projector captures were inspected.
- Vercel production deployment `dpl_Cr7yQnDpDiq5GeBfnrkFonvALqtq` is `READY` and aliased to `https://hisn-ot-prototype.vercel.app`. Public `/`, `/healthz`, and `/readyz` requests returned HTTP 200. Authenticated public rehearsals completed: the safe request reached `ALLOW`, accepted 52%, and evolved to 47.876% actual after three simulated seconds; the unsafe request reached `BLOCK_AND_CONTAIN`, kept accepted and actual pressure at 46%, and recorded zero unsafe executions.
- Public rehearsals returned `SANDBOX` evidence for SIM Swap, Device Swap, Location Verification, and Device Reachability. Number Verification recorded a reason-bearing `SIMULATED` fallback because subscriber authorization is absent. QoD created, polled, and released a Nokia test session, remained `REQUESTED`, and was recorded `PENDING`; backup ownership was withheld and the modeled pump safe-stopped. Slice detachment remained a reason-bearing simulation because no attachment is configured.

The review fixed only observed defects and claim drift. It does not assert exhaustive security, safety, telecom, model-provider, deployment, or industrial-control assurance.

Scope: application/domain/adapters/persistence/UI/tests/configuration/docs. This ledger records confirmed defects and evidence, not an exhaustive security assessment.
